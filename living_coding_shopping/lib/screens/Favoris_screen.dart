import 'package:flutter/material.dart';

class FavorisScreen extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Favoris"),),
      body: Center(child: Text("Nos Favoris"),),
      
    );
  }
}